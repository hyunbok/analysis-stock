import 'package:flutter/material.dart';

/// AI 자동매매 대시보드 — 마스터 스위치, 장세, 전략, 손익.
/// TODO(ai_trading): 실제 UI 구현
class AiTradingScreen extends StatelessWidget {
  const AiTradingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AI Trading — TODO')),
    );
  }
}
